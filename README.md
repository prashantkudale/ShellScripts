# ShellScripts

# AmazoneLinuxEC2.sh - This file contains the code which will install Java and Jenking for AWS Linux Machine. We need to add this detail at the User Data so this will install the Java And Jenkins at the time of creation of instance.
